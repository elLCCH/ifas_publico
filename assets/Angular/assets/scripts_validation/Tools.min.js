$(document).ready(function() {
  $('.dropdown-submenu').on('click', function(e) {
    if ($(e.target).closest('[data-name="noCerrar"]').length) {
      // Agrega excepción para no cerrar el dropdown cuando se hace clic en un elemento con el nombre "elemento1"
      e.stopPropagation();
    }
  });
});
